clear

kernelSize = 5;
kernelMaximumIndex = (kernelSize - 1) / 2;
kernelRange = -kernelMaximuIndex:kernelMaximuIndex
sigma = 1;

[x, y] = meshgrid(-(m-1)/2:(m-1)/2, -(n-1)/2:(n-1)/2);

gaussian = exp(- (x.^2+y.^2) / (2*sigma^2));

gaussianNormalized = gaussian ./ sum(gaussian(:));

figure()
subplot(1, 2, 1)
surf(x, y, gaussian);

subplot(1, 2, 2)
surf(x, y, gaussianNormalized);

