

// ------------------------------------------------------------------------// 
//      TypeScript Typings                                                 //
// ------------------------------------------------------------------------//

var uint8Array = new Uint8Array((<any>fileDataObj.target).result);
    EventTarget does not contain a member result.
    https://github.com/Microsoft/TSJS-lib-generator/pull/207

// ------------------------------------------------------------------------// 
//      Questions                                                          //
// ------------------------------------------------------------------------//

Which content (e.g. scripts, etc.) should be in wwwroot?
    Google Chrome (and other browsers?) cannot resolve references in the TypeScript map file to source files outside the wwwwroot folder.
        So, the TypeScript files must also be located below wwwroot.

    How is the server configure to allow access to outside wwwroot?

What is the correct way to add typings?

When NPM is used to install typings (e.g. dat-gui) where are the files installed?
    The included types were still unresolved...
